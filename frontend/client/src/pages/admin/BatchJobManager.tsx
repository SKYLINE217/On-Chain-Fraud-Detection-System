export default function BatchJobManager() {
  return (
    <div className="flex flex-col gap-6 animate-in fade-in duration-slow">
      <h1 className="text-2xl font-bold">Batch Job Manager</h1>
      <p className="text-secondary text-body max-w-2xl">
        Trigger manual graph updates and inference pipelines (WIP).
      </p>
    </div>
  );
}

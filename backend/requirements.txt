# ── BUG-35 Fix: Pin all dependencies to avoid breaking changes on fresh installs ──
# Format: package==major.minor.patch
# Regenerate with: pip freeze > requirements.txt after testing a working install.

# ── Shared / Environment ─────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.7.4

# ── Web API / Serving ────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
requests==2.32.3
slowapi==0.1.9

# ── Database / Cache ─────────────────────────────────────────────────────
neo4j==5.21.0
redis==5.0.7

# ── Data Processing ──────────────────────────────────────────────────────
pyarrow==16.1.0
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1

# ── ML / GNN ────────────────────────────────────────────────────────────
# Note: torch, torch-geometric, etc. require platform-specific wheels.
# Install via: pip install torch==2.3.1 torch_geometric==2.5.3
# See: https://pytorch.org/get-started/locally/
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1
torch_geometric==2.5.3

xgboost==2.0.3
scikit-learn==1.5.1
shap==0.45.1

# ── Experiment Tracking / Visualization ──────────────────────────────────
wandb==0.17.3
streamlit==1.36.0
plotly==5.22.0
pyvis==0.3.2

# ── Testing & Load Testing ───────────────────────────────────────────────
pytest==8.2.2
locust==2.29.0
